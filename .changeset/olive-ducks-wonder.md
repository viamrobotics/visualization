---
'@viamrobotics/visualization': patch
---

Allow editing a fragment component's frame unless a fragment variable actually supplies one of its fields, rather than whenever the fragment declares any variable.
