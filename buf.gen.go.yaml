version: v2
managed:
  enabled: false
plugins:
  # Go code generation
  - local: protoc-gen-go
    out: .
    opt: paths=source_relative
  # Connect-RPC code generation (gRPC-compatible service stubs)
  - local: protoc-gen-connect-go
    out: .
    opt: paths=source_relative
